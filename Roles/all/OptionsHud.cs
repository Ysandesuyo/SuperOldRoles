using HarmonyLib;
using System;
using System.Linq;
using UnityEngine;

namespace SuperOldRoles.Roles.all
{/*  開発中
    [HarmonyPatch(typeof(GameOptionsMenu))]
    public static class OptionsHud
    {
        public static ToggleOption showJesterOption;
        public static NumberOption JesterAmount;

        [HarmonyPatch(nameof(GameOptionsMenu.Awake))]
        public static void Postfix(GameOptionsMenu __instance)
        {
            
        }
    }*/
}
